**Written 3 files:**

| File | Purpose |
|------|---------|
| `adr/0001-single-file-past-500-loc.md` | Single file at ~1000 LOC vs 500 guideline; defer split post-wave |
| `adr/0002-speculative-session-control-bridge.md` | `/send-worker` built against non-existent pi CLI flags; fallback-first UX |
| `wave-02/adr-collector.md` | Collection report with candidate evaluation |

**Two ADRs recorded, two candidates skipped** (drift constant + registry storage follow established wave-01 patterns). Batch-02 scoping failure and cross-namespace slot collision noted as process observations, pas as architectural decisions.